import React, { FC } from 'react';
import { TestProps } from './types';
import styles from './index.css';

export const Test: FC<TestProps> = (props) => {
  return <h1 className={styles.root}>Test</h1>;
};
