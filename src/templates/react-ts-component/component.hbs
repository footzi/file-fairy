import { React } from 'react;
import {{ name }}Props from './types';
{{#if options.useCssModules}}
import styles from './index.css';
{{/if}}

{{#if options.useCssModules}}
export const {{ name }}: React.FC<{{name}}Props> = (props) => {
    return <h1 className={styles.root}>{{name}}</h1>
}
{{else}}
export const {{ name }}: React.FC<{{name}}Props> = (props) => {
    return <h1>{{name}}</h1>
}
{{/if}}
