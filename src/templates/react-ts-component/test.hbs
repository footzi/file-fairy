import { render } from '@testing-library/react';
import { {{uppercaseFirstLetter name}} } from './index';

describe('{{uppercaseFirstLetter name}}', () => {
    it('should render', () => {
      render({{uppercaseFirstLetter name}});
    });
});
