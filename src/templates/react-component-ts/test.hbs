import { render } from '@testing-library/react';
import { {{uppercaseFirstLetter (toCamelCase name)}} } from '../index';

describe('{{uppercaseFirstLetter (toCamelCase name)}}', () => {
  it('should render', () => {
    render({{uppercaseFirstLetter (toCamelCase name)}});
  });
});
