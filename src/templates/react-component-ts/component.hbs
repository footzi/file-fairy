import React, { FC } from 'react';
{{#if options.useTypesInComponent}}
{{else}}
import { {{ name }}Props } from './types';
{{/if}}
{{#if options.noStyle}}
{{else}}
import styles from './index.css';
{{/if}}

{{#if options.useTypesInComponent}}
  {{#if options.useTypeAsKeyWord}}
type {{uppercaseFirstLetter name}}Props = {}
  {{else}}
interface {{uppercaseFirstLetter name}}Props {}
  {{/if}}

{{/if}}
{{#if options.noStyle}}
export const {{ uppercaseFirstLetter name }}: FC<{{name}}Props> = (props) => {
  return <h1>{{name}}</h1>;
};
{{else}}
export const {{ uppercaseFirstLetter name }}: FC<{{name}}Props> = (props) => {
  return <h1 className={styles.root}>{{name}}</h1>;
};
{{/if}}
