import {{name}} from './index';

describe('{{name}}', () => {
  it('should return correct result', () => {
    const props = {};
    const result = {};

    expect({{name}}(props)).toBeEqual(result);
  });
});

