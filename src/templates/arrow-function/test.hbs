import { {{toCamelCase name}} } from '../index';

describe('{{toCamelCase name}}', () => {
  it('should return correct result', () => {
    const props = {};
    const expectedResult = {};

    const result = {{toCamelCase name}}(props);

    expect(result).toEqual(expectedResult);
  });
});
