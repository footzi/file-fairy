{{#if options.useTypesInComponent}}
{{else}}
import { {{uppercaseFirstLetter (toCamelCase name)}}Props, {{uppercaseFirstLetter (toCamelCase name)}}Result } from './types';

{{/if}}
{{#if options.useTypesInComponent}}
  {{#if options.useTypeAsKeyWord}}
type {{uppercaseFirstLetter (toCamelCase name)}}Props = {}

type {{uppercaseFirstLetter (toCamelCase name)}}Result = {}

  {{else}}
interface {{uppercaseFirstLetter (toCamelCase name)}}Props {}

interface {{uppercaseFirstLetter (toCamelCase name)}}Result {}

  {{/if}}
{{/if}}
export const {{toCamelCase name}} = (props: {{uppercaseFirstLetter (toCamelCase name)}}Props): {{uppercaseFirstLetter (toCamelCase name)}}Result => {
  return 'hello';
};
