import React from 'react';
import './styles.scss';

const {{ uppercaseFirstLetter (toCamelCase name) }} = (props) => {
  return (
    <>
      <h1>Hello</h1>
      <h2>You can use your variables in templates</h2>
      <p>
        Write:
        <code>`ff gc my-temp ./src/Test --myVariable=name --isGood`</code>
      </p>
      <p>And then use in template {{vars.myVariable}} and as boolean {{vars.isGood}}</p>
    </>
  );
};

export default {{ uppercaseFirstLetter (toCamelCase name) }};
